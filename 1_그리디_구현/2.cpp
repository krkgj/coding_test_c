// 그리디 $ 구현 - 2. 1이 될 때까지
#include <bits/stdc++.h>

using namespace std;

int n, k = 0;

int main(void) {
    cin >> n >> k;
    while (1) {
        if (n >= k) {
            if (n % k == 0) {
                
            }
        }
    }
}